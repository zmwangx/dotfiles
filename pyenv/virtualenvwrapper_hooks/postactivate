# This hook is sourced after every virtualenv is activated.

export PYTHON="$VIRTUAL_ENV/bin/python"
VIRTUAL_ENV_PYTHON_VERSION="$($PYTHON --version 2>&1)"
VIRTUAL_ENV_PYTHON_VERSION="${VIRTUAL_ENV_PYTHON_VERSION#Python }"
